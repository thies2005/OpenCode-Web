services:
  opencode:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - opencode-config:/root/.config/opencode
      - opencode-local:/root/.local/share/opencode
      - opencode-home:/root/.opencode
      - workspace:/workspace
    networks:
      - coolify
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "coolify.port=4096"
      - "traefik.http.middlewares.opencode-auth.basicauth.users=opencode:$2a$10$36FV0r0DzXS/5hthV59NP.CMIU3VSxyM168h5gnQKbPF3Y.kzzyyS"

volumes:
  opencode-config:
  opencode-local:
  opencode-home:
  workspace:

networks:
  coolify:
    external: true
